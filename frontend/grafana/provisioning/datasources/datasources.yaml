# MOCK MODE placeholder: this stack has no lab, no backend, and no
# metrics/log store, so no datasource is provisioned. A future "api"
# mode would add real entries here (e.g. VictoriaMetrics, Loki), but
# only once those services actually exist in docker-compose.yml —
# do not point at container addresses that aren't part of this stack.
apiVersion: 1
datasources: []
